apiVersion: v1
kind: Service
metadata:
  name: api-port
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
